spring:
  datasource:
    url: jdbc:h2:mem:edu_rag;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate

edu-rag:
  ai-service:
    base-url: http://127.0.0.1:8765
    connect-timeout: 1s
    read-timeout: 1s
